# Add files/folders to ignore for Prettier, e.g.:
# node_modules
# build
# dist
service-account.json
serviceAccount.local.json

# NDJSON / JSON Lines (not a single JSON document)
etl/player_stats_footywire.json
