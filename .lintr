# Completely disable linting for problematic file patterns
# This prevents VS Code chat interface from triggering R linter errors

linters: NULL

exclusions: list(
  "chat-editing" = list(),
  "modified-file-entry" = list(),
  "chat-editing-snapshot-text-model" = list(),
  "chat-editing-text-model" = list()
)
