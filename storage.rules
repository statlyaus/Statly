rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      // Restrict emulator to signed-in users; tighten further per feature needs.
      allow read, write: if request.auth != null;
    }
  }
}
